name: tutor_app
description: A Flutter tutor app for preschool to class 5 students
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.2   # optional; can keep or remove
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - .env
